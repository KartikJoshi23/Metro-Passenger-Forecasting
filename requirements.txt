# Dubai Metro Forecasting — Python pipeline / notebook
# NOTE: use Python 3.10–3.12. TensorFlow has no wheels for 3.13/3.14 yet, so installs fail there.
# (This file is for the model pipeline only — the Netlify/Pages dashboard build needs NO Python.)
numpy
pandas
pyarrow
scikit-learn
matplotlib
tensorflow-cpu>=2.16   # provides tensorflow.keras (LSTM, SimpleRNN, GRU, Conv1D)
# notebook tooling (optional, to run/convert the .ipynb)
nbformat
nbconvert
ipykernel
